name: BIST Web Sitesini ve Verileri Otomatik Guncelle

on:
  push:
    branches:
      - main
  schedule:
    # Hafta ici borsa acilmadan once gunluk fon ve seans oncesi guncelleme (06:30 UTC = 09:30 TSI)
    - cron: '30 6 * * 1-5'
    # Hafta ici borsa seans saatlerinde her saat basi calisir (07:00 - 16:00 UTC = 10:00 - 19:00 TSI)
    - cron: '0 7-16 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Kodu Klonla
        uses: actions/checkout@v4

      - name: Node.js Kur
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Paketleri Kur
        run: npm ci || npm install

      - name: Testleri Calistir
        run: npm test

      - name: Canli Verileri Cek ve data.json Uret
        run: node fetch-data.js

      - name: Dagitim Dizinini Hazirla (_site)
        run: |
          mkdir -p _site
          cp -r index.html hisse.html portfoy.html hesaplayici.html abone.html favicon.svg favicon.png favicon.ico apple-touch-icon.png data.json data _site/
          touch _site/.nojekyll

      - name: GitHub Pages Icin Hazirla
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: GitHub Pagese Yayinla
        id: deployment
        uses: actions/deploy-pages@v4